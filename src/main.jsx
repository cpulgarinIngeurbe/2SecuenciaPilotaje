import React from "react";
import ReactDOM from "react-dom/client";
import PileScheduler from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PileScheduler />
  </React.StrictMode>
);
